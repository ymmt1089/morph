
<div class="sign_main">
  <div class="circle_sign_1"></div>
  <div class="circle_sign_2"></div>


<div class="sign_container">
    <h4 class="sign_title">パスワード再設定画面</h4>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div id="create_password" class='input-field'>
          <i class="icon-prepend fa fa-user" ></i>
          <%= f.password_field :password , autofocus: true ,:html => {:id => "input_password" ,:type => "text"}%>
          <label for="input_a">新しいパスワード</label>
      </div>

      <div id="create_password" class='input-field'>
          <i class="icon-prepend fa fa-user" ></i>
          <%= f.password_field :password_confirmation , autofocus: true ,:html => {:id => "input_password" ,:type => "text"}%>
          <label for="input_a">新しいパスワード(確認用)</label>
      </div>

        <div class="submit_button">
          <%= f.submit 'パスワード再設定' , id:'submit', data: {disable_with: 'ログイン中...'} ,:class=>"btn waves-effect"%>
        </div>


        <div class="render_box">
        <%= render "users/shared/links" %>
        </div>
    <% end %>
  </div>
</div>
